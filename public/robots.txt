User-agent: *
Allow: /

# 不阻止 CSS / JS / Images 等关键资源
# 保持渲染所需的资源可访问，便于通过移动端抓取与渲染

# 仅屏蔽低价值或敏感路径（非资源文件）
Disallow: /search
Disallow: /admin
Disallow: /api
Disallow: /*?*filter=
Disallow: /*?*sort=

# 法务等页面允许抓取（但建议不纳入站点地图的优先抓取清单）
Allow: /privacy-policy/
Allow: /terms-of-service/

# 站点地图（索引文件）位置
Sitemap: https://actionfigure-generator.com/sitemap.xml
